<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         displayDetailsOnTestsThatTriggerWarnings="true"
         failOnRisky="true"
         failOnWarning="true"
         defaultTestSuite="default">
    <testsuites>
        <testsuite name="default">
            <directory>tests</directory>
            <!-- Byte-exact rendering varies between GD builds that report the same version,
                 so it is reviewed locally via the golden suite, never in CI. -->
            <exclude>tests/Golden</exclude>
        </testsuite>
        <testsuite name="golden">
            <directory>tests/Golden</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
